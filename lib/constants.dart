import 'package:flutter/foundation.dart';

const remoteHttpDomain =
    !kDebugMode ? "https://www.gatorapi.win" : "https://www.gatorapi.win";
const remoteWsDomain =
    !kDebugMode ? "wss://gatorapi.win" : "wss://gatorapi.win";

const clientID = kDebugMode ? "30bf4172998cc4ec684e" : "30bf4172998cc4ec684e";
