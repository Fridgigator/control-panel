import 'package:flutter/foundation.dart';

const remoteHttpDomain =
    kDebugMode ? "http://localhost:1234" : "https://fridgigator-001.fly.dev";
const remoteWsDomain =
    kDebugMode ? "ws://localhost:1234" : "wss://fridgigator-001.fly.dev";
